{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openagents.dev/schemas/tool-spec.schema.json",
  "title": "ToolSpec",
  "description": "Definition of a tool exposed through the tool gateway.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "description",
    "input_schema",
    "output_schema",
    "permissions",
    "side_effects"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Tool identifier.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "Purpose and expected behavior of the tool.",
      "minLength": 1
    },
    "input_schema": {
      "type": "object",
      "description": "JSON Schema for tool input payload."
    },
    "output_schema": {
      "type": "object",
      "description": "JSON Schema for tool output payload."
    },
    "permissions": {
      "type": "array",
      "description": "Capabilities required to invoke the tool.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "side_effects": {
      "type": "string",
      "description": "External effects caused by tool execution.",
      "enum": [
        "none",
        "fs",
        "network",
        "external"
      ]
    }
  }
}
